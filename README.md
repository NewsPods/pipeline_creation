# pipeline_creation
this repo is totally about the pipeline creation of our project NEWSPODS where the csv will be first pass through ssml creation each by each and than with the ssml an audio will be systhesiised using Azure TTS and than the audio file is uploaded in backblaze bucket and the corresponding details are stored in cockroach DB
